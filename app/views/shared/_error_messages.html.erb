<% if model.errors.any? %>
  <div style="color: red">
    <p><%= model.model_name.human %>に<%= model.errors.count %>個のエラーが発生しました</p>

    <ul>
      <% model.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

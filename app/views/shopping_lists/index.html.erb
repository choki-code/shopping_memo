<p style="color: green"><%= notice %></p>

<% content_for :title, "買い物リスト" %>

<h1>買い物リスト</h1>

<div id="shopping_lists">
  <% @shopping_lists.each do |shopping_list| %>
    <%= render shopping_list %>
    <p>
      <%= link_to "このリストを見る", shopping_list %>
    </p>
  <% end %>
</div>

<%= link_to "リストを新規作成", new_shopping_list_path %>
